# TODO - Fix “limit” / spacing and form-page behavior

- [x] Update `script.js` so logo click scrolls to `#hero` only if it exists (prevents issues on `form.html`).
- [ ] Adjust `LAND.CSS` hero sizing: reduce/remove `min-height` on mobile so layout doesn’t feel capped.

- [ ] Adjust `form.css` `min-height: 100vh` if it causes the form page to feel constrained.
- [ ] (Optional) Verify anchor/scroll alignment with sticky navbar (if user reports overlap).


